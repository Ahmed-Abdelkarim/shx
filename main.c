#include <stdio.h>
#include <cstdlib>

int main(int argc, char *argv[]) {

    // Initialize the shell

    // loop to process commands
    printf("Hello, World!\n");

    // Cleanup and exit
    return EXIT_SUCCESS;
}